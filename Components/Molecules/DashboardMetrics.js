import React, { Fragment } from "react";
import MetricNeomorph from "../Atoms/MetricNeomorph";

const DashboardMetrics = ({ goals }) => {
  return (
    <Fragment>
      <MetricNeomorph text={"test"} />
    </Fragment>
  );
};

export default DashboardMetrics;
